# Pre-PR implementation review — kanban card open

Date: 2026-06-27
Branch: main
Base/range: origin/main...HEAD plus uncommitted working-tree diff
Plan/scope: Standalone direct fix — remove redundant `Details / Open` link from Kanban plan cards; clicking non-interactive card content opens the plan; click-and-drag still allows Kanban drag/drop.

## Review packet

### Status

```text
## main...origin/main
 M src/__tests__/contracts.test.ts
 M src/server/app.ts
 M src/test-fixtures/e2e-run.ts
```

### Committed diff against base

```text
# git diff --stat origin/main...HEAD
# git diff --name-only origin/main...HEAD
(no committed diff)
```

### Working-tree diff

```text
 src/__tests__/contracts.test.ts |  7 +++++--
 src/server/app.ts               |  6 +++---
 src/test-fixtures/e2e-run.ts    | 13 ++++++++++++-
 3 files changed, 20 insertions(+), 6 deletions(-)
```

Changed files:

```text
src/__tests__/contracts.test.ts
src/server/app.ts
src/test-fixtures/e2e-run.ts
```

### Staged diff

```text
(no staged diff)
```

## Verification before review

- `bun run build` — PASS
- `node --test dist/__tests__/contracts.test.js` — PASS, 143 tests
- `bun run test:e2e` — PASS, e2e scenarios passed

## Review cycle 1

- GLM verdict: `CLEAN_FOR_PR`
  - Scope: three unstaged changed files; direct Kanban card open/link removal behavior.
  - Coverage: card markup/styles, click/keyboard behavior, drag/drop lifecycle, context menu/existing interactions, tests/fixtures, and scoped security/data-loss/perf/concurrency checks.
  - Findings: none.
- GPT verdict: `CLEAN_FOR_PR`
  - Scope: same three unstaged implementation/test files plus this untracked validation note.
  - Coverage: Kanban markup, click handling, keyboard behavior, drag/drop lifecycle, context menu/existing interactive elements, contract assertions, e2e fixture truthfulness, and verification.
  - Findings: none.

## Triage

| Finding | Reviewer | Severity | Scope | Decision | Evidence |
| --- | --- | --- | --- | --- | --- |
| None | GLM | — | — | Clean | GLM found no issues across markup, click/keyboard, drag/drop, context menu, and tests. |
| None | GPT | — | — | Clean | GPT found no issues across markup, click handling, keyboard behavior, drag/drop, context menu, tests, and verification. |

## Fixes applied

None after review. Both reviewers returned clean verdicts with no in-scope P1/P2/P3 findings.

## Verification after fixes

No post-review fixes were required. Latest relevant verification remains:

- `bun run build` — PASS
- `node --test dist/__tests__/contracts.test.js` — PASS, 143 tests
- `bun run test:e2e` — PASS, e2e scenarios passed
- Final pre-PR verification after reviewer gate: `bun run test && bun run test:e2e` — PASS, 144 tests plus e2e scenarios passed.

GPT reviewer also independently ran `bun run build && node --test dist/__tests__/contracts.test.js && bun run test:e2e` and reported PASS.

## PR feedback iteration

Codex PR review on commit `6349b64` reported one P2 issue: clicking a Kanban card body while the context menu is open dismissed the menu and then fell through to card navigation. Fixed by returning immediately after `closeKanbanMenu()` in the document click handler. Added contract coverage for the early return and e2e coverage proving card-body dismissal leaves the browser on the Kanban page.

Verification after PR feedback fix:

- `bun run build` — PASS
- `node --test dist/__tests__/contracts.test.js` — PASS, 143 tests
- `bun run test:e2e` — PASS, e2e scenarios passed

## Remaining follow-ups

None.

## Final gate result

- GPT verdict: `CLEAN_FOR_PR`
- GLM verdict: `CLEAN_FOR_PR`
- Gate status: `OPEN_PR_READY`
