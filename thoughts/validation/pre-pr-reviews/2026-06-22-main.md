# Pre-PR Implementation Review — board column rename/hide

Date: 2026-06-22
Branch/worktree reviewed: `main` at `/Users/anichols/code/plan-reviewer`
Base/range: `origin/main` plus unstaged working-tree changes
Plan/scope: standalone user request — board column rename must update the stable key applied to plans, and columns with assigned plans must be hideable so hidden columns and their plans disappear from Kanban while assignments remain intact.

## Review packet

`git status --short --branch`:

```text
## main...origin/main
 M src/__tests__/contracts.test.ts
 M src/cli.ts
 M src/schemas.ts
 M src/server/app.ts
 M src/storage/database.ts
?? thoughts/plans/pi-plan-mode-registration-monitor-fix.html
```

Committed diff vs `origin/main`: none.

Unstaged implementation diff:

```text
src/__tests__/contracts.test.ts | 76 ++++++++++++++++++++++++++++++++++++-----
src/cli.ts                      | 11 ++++--
src/schemas.ts                  |  1 +
src/server/app.ts               | 13 +++----
src/storage/database.ts         | 29 +++++++++++-----
5 files changed, 104 insertions(+), 26 deletions(-)
```

Changed files in scope:

- `src/storage/database.ts`
- `src/server/app.ts`
- `src/schemas.ts`
- `src/cli.ts`
- `src/__tests__/contracts.test.ts`

Untracked unrelated file excluded from review scope:

- `thoughts/plans/pi-plan-mode-registration-monitor-fix.html`

## Cycle 1 verdicts

- GPT verdict: `P1_P2_FOUND`
- GLM verdict: `CLEAN_FOR_PR` by gate severity, with the same lifecycle issue classified as P3.

### Triage

| Finding | Reviewer | Severity | Scope | Decision | Evidence |
| --- | --- | --- | --- | --- | --- |
| Hidden-column assignment was remapped to a visible default on active transition, resume, or unarchive | GPT | P2 | REGRESSION_FROM_THIS_DIFF | Fixed | `visibleBoardColumnKey()` only checked visible columns, while the feature now permits valid plans assigned to hidden columns. |
| Hidden-column assignment lifecycle behavior was newly reachable and untested | GLM | P3 | REGRESSION_FROM_THIS_DIFF | Fixed as part of GPT P2 | Same reachable path; fixed to preserve hidden-column assignment and covered with tests. |
| Multi-column swap/chained renames require multiple saves | GLM | P3 | IN_PLAN / UX limitation | Deferred | Valid input fails closed with a clear 400; normal single-column UI/CLI rename works. Tracking: this artifact follow-up list. |
| CLI cannot rename hidden columns because it fetches visible-only columns | GLM | P3 | OUT_OF_SCOPE_FOLLOW_UP | Deferred | CLI-visible limitation with a clear error; UI can rename hidden columns. Tracking: this artifact follow-up list. |

## Fixes applied after cycle 1

- Updated `src/storage/database.ts` so lifecycle restore paths preserve `board_column_key` when it exists in `board_columns`, including hidden columns.
- Extended `src/__tests__/contracts.test.ts` to cover hidden-column assignment across defer/resume and archive/unarchive.
- Updated stale assertions to match the intended preservation semantics.

## Verification after fixes

```bash
bun run build && node --test dist/__tests__/contracts.test.js --test-name-pattern "organization APIs|board column rename|hidden backlog"
```

Result: full contracts file ran; 119 pass, 0 fail.

## Cycle 2 verdicts

- GPT verdict: `CLEAN_FOR_PR`
- GLM verdict: `CLEAN_FOR_PR`

### Remaining non-blocking findings

| Finding | Reviewer | Severity | Scope | Decision | Tracking / evidence |
| --- | --- | --- | --- | --- | --- |
| Multi-column swap/chained renames require multiple saves | GLM | P3 | OUT_OF_SCOPE_FOLLOW_UP | Deferred | Clear 400 rollback, no data corruption; track here as future UX improvement. |
| Plan detail shell may show raw hidden-column key instead of label | GLM | P3 | OUT_OF_SCOPE_FOLLOW_UP | Deferred | Cosmetic only; fallback shows key, not unassigned; track here as future UI polish. |
| Reviewed changes are unstaged on `main`, not committed to the `fix-links` worktree | GLM | QUESTION / operational note | QUESTION | Not a code blocker | Commit/cherry-pick reviewed diff onto the intended PR branch before opening a PR. |

## Final verification

```bash
bun run test
```

Result: 120 pass, 0 fail.

## Final gate result

PASS — both reviewers report no unresolved in-scope P1/P2 issues.

- GPT verdict: `CLEAN_FOR_PR`
- GLM verdict: `CLEAN_FOR_PR`
- Verification after last fix: `bun run test` passed, 120/120.
